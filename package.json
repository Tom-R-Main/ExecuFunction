{
  "name": "execufunction",
  "private": true,
  "workspaces": [
    "exf-app"
  ],
  "scripts": {
    "build": "npm run build --workspace exf-app",
    "start": "npm run start --workspace exf-app",
    "start:dev": "npm run start:dev --workspace exf-app",
    "lint": "npm run lint --workspace exf-app",
    "test": "npm test --workspace exf-app"
  }
}
